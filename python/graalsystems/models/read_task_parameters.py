# coding: utf-8

"""
    Tenant API

    Tenant API

    The version of the OpenAPI document: 0.0.1
    Contact: abc@layer.fr
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
from inspect import getfullargspec
import json
import pprint
import re  # noqa: F401

from typing import Any, List, Optional
from pydantic import BaseModel, Field, StrictStr, ValidationError, field_validator
from graalsystems.models.load_model_params import LoadModelParams
from graalsystems.models.read_file_params import ReadFileParams
from graalsystems.models.read_sql_params import ReadSQLParams
from typing import Union, Any, List, TYPE_CHECKING, Optional, Dict
from typing_extensions import Literal
from pydantic import StrictStr, Field
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

READTASKPARAMETERS_ONE_OF_SCHEMAS = ["LoadModelParams", "ReadFileParams", "ReadSQLParams"]

class ReadTaskParameters(BaseModel):
    """
    Parameters of the read task.
    """
    # data type: ReadFileParams
    oneof_schema_1_validator: Optional[ReadFileParams] = None
    # data type: ReadSQLParams
    oneof_schema_2_validator: Optional[ReadSQLParams] = None
    # data type: LoadModelParams
    oneof_schema_3_validator: Optional[LoadModelParams] = None
    actual_instance: Optional[Union[LoadModelParams, ReadFileParams, ReadSQLParams]] = None
    one_of_schemas: List[str] = Literal["LoadModelParams", "ReadFileParams", "ReadSQLParams"]

    model_config = {
        "validate_assignment": True
    }


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ReadTaskParameters.model_construct()
        error_messages = []
        match = 0
        # validate data type: ReadFileParams
        if not isinstance(v, ReadFileParams):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ReadFileParams`")
        else:
            match += 1
        # validate data type: ReadSQLParams
        if not isinstance(v, ReadSQLParams):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ReadSQLParams`")
        else:
            match += 1
        # validate data type: LoadModelParams
        if not isinstance(v, LoadModelParams):
            error_messages.append(f"Error! Input type `{type(v)}` is not `LoadModelParams`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ReadTaskParameters with oneOf schemas: LoadModelParams, ReadFileParams, ReadSQLParams. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ReadTaskParameters with oneOf schemas: LoadModelParams, ReadFileParams, ReadSQLParams. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: dict) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ReadFileParams
        try:
            instance.actual_instance = ReadFileParams.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ReadSQLParams
        try:
            instance.actual_instance = ReadSQLParams.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into LoadModelParams
        try:
            instance.actual_instance = LoadModelParams.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ReadTaskParameters with oneOf schemas: LoadModelParams, ReadFileParams, ReadSQLParams. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ReadTaskParameters with oneOf schemas: LoadModelParams, ReadFileParams, ReadSQLParams. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        to_json = getattr(self.actual_instance, "to_json", None)
        if callable(to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> dict:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        to_dict = getattr(self.actual_instance, "to_dict", None)
        if callable(to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


