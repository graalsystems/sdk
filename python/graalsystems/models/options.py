# coding: utf-8

"""
    Tenant API

    Tenant API

    The version of the OpenAPI document: 0.0.1
    Contact: abc@layer.fr
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json
import importlib


from typing import Dict, Optional, Union
from pydantic import BaseModel, StrictStr
from typing import Dict, Any
try:
    from typing import Self
except ImportError:
    from typing_extensions import Self

class Options(BaseModel):
    """
    Options
    """
    env: Optional[Dict[str, StrictStr]] = None
    docker_image: Optional[StrictStr] = None
    instance_type: Optional[StrictStr] = None
    type: Optional[StrictStr] = None
    __properties: ClassVar[List[str]] = ["env", "docker_image", "instance_type", "type"]

    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }


    # JSON field name that stores the object type
    __discriminator_property_name: ClassVar[List[str]] = 'type'

    # discriminator mappings
    __discriminator_value_class_map: ClassVar[Union[Dict[str, str], None]] = None

    @classmethod
    def _get_discriminator_value_class_map(cls) -> ClassVar[Dict[str, str]]:
        if cls.__discriminator_value_class_map == None:
            # Prevent circular imports caused by mutually referencing classes
            globals()["airflow"] = importlib.import_module("graalsystems.models.airflow").airflow
            globals()["bash"] = importlib.import_module("graalsystems.models.bash").bash
            globals()["dask"] = importlib.import_module("graalsystems.models.dask").dask
            globals()["database-migration"] = importlib.import_module("graalsystems.models.database_migration").database-migration
            globals()["dbt"] = importlib.import_module("graalsystems.models.dbt").dbt
            globals()["docker"] = importlib.import_module("graalsystems.models.docker").docker
            globals()["flink"] = importlib.import_module("graalsystems.models.flink").flink
            globals()["hadoop"] = importlib.import_module("graalsystems.models.hadoop").hadoop
            globals()["knime"] = importlib.import_module("graalsystems.models.knime").knime
            globals()["lowcode"] = importlib.import_module("graalsystems.models.lowcode").lowcode
            globals()["mxnet"] = importlib.import_module("graalsystems.models.mxnet").mxnet
            globals()["python"] = importlib.import_module("graalsystems.models.python").python
            globals()["pytorch"] = importlib.import_module("graalsystems.models.pytorch").pytorch
            globals()["ray"] = importlib.import_module("graalsystems.models.ray").ray
            globals()["sas"] = importlib.import_module("graalsystems.models.sas").sas
            globals()["spark"] = importlib.import_module("graalsystems.models.spark").spark
            globals()["tensorflow"] = importlib.import_module("graalsystems.models.tensorflow").tensorflow
            globals()["xgboost"] = importlib.import_module("graalsystems.models.xgboost").xgboost

            cls.__discriminator_value_class_map = {
                'airflow': 'AirflowOptions','bash': 'BashOptions','dask': 'DaskOptions','database-migration': 'DatabaseMigrationOptions','dbt': 'DbtOptions','docker': 'DockerOptions','flink': 'FlinkOptions','hadoop': 'HadoopOptions','knime': 'KnimeOptions','lowcode': 'LowCodeOptions','mxnet': 'MXNetOptions','python': 'PythonOptions','pytorch': 'PyTorchOptions','ray': 'RayOptions','sas': 'SASOptions','spark': 'SparkOptions','tensorflow': 'TensorflowOptions','xgboost': 'XgboostOptions'
            }
        return cls.__discriminator_value_class_map

    @classmethod
    def get_discriminator_value(cls, obj: dict) -> str:
        """Returns the discriminator value (object type) of the data"""

        discriminator_value = obj[cls.__discriminator_property_name]
        if discriminator_value:
            return cls._get_discriminator_value_class_map().get(discriminator_value)
        else:
            return None

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Union[Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self]:
        """Create an instance of Options from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        _dict = self.model_dump(
            by_alias=True,
            exclude={
            },
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> Union[Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self, Self]:
        """Create an instance of Options from a dict"""
        # look up the object type based on discriminator mapping
        object_type = cls.get_discriminator_value(obj)
        if object_type:
            klass = globals()[object_type]
            return klass.from_dict(obj)
        else:
            raise ValueError("Options failed to lookup discriminator value from " +
                             json.dumps(obj) + ". Discriminator property name: " + cls.__discriminator_property_name +
                             ", mapping: " + json.dumps(cls._get_discriminator_value_class_map()))


